# Microsoft Entra ID — same tenant the shell SPA authenticates against
ENTRA_TENANT_ID=
ENTRA_AUDIENCE=

# Optional: override the JWKS URL (defaults to the common Entra v2.0 endpoint)
# ENTRA_JWKS_URL=

# Set to 1 to bypass JWT verification (local dev only)
SKIP_AUTH=0
